export const validateSignIn = (email, password, name, phoneNo) => {
    // const isEmailValid = /^[\w-.]+@([\w-]+\.)+[\w-]{2,4}$/.test(email);
    // const isPasswordValid = /^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[a-zA-Z]).{8,}$/.test(password);
    // const isNameValid = /\b([A-ZÀ-ÿ][-,a-z. ']+[ ]*)+/.test(name);
    // const isPhoneNoValid = /^(\+\d{1,2}\s?)?1?-?\.?\s?\(?\d{3}\)?[\s.-]?\d{3}[\s.-]?\d{4}$/.test(phoneNo);

    // if(!isEmailValid) return "Email is not valid";
    // if(!isPasswordValid) return "Password is not valid";
    // if(!isNameValid) return "Please enter valid name";
    // if(!isPhoneNoValid) return "Please enter valid phone number";
    
    return null;
};
